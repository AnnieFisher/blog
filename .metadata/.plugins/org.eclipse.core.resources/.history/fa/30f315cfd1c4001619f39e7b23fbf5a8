
	
	<div class="container">
        <div class="row">
            <div class="col-md-6 col-md-offset-3"><h1 class="h1-responsive" id = "mainTitle">Annie's World of Inspiration </h1></div>
            </div>
            </div>
       <div class="container">
        <div class="row">
            <div class="col-md-4" id ="transparent">
	            		<ul>
	            		<li>
	            		<h4 id="quote">"{{temp}}"</h4>
	            		<h5 id = "author" ng-repeat= "qoute in quotes">-{{qoute.title}}</h5>
	            		</li>
	            		</ul>
            </div>
           
		</div>
    </div>
    
    
   <div class = "container-fluid" style="margin-top:50px">
		<div id = "add" >
			<button class="btn btn-success" ng-click = "addPostRedirect()" >Add Post</button>
		</div>
	</div><br>



     <div class="container">      
  
       <div class="row">
        		<div>
	        		<ul ng-repeat= "post in posts">
					<div class="border">
						<li id="title" ng-click = goToBlog(post)>
							<h3 id="name">{{post.postName}}</h3>
							<h5 id ="date">{{post.postDate}}</h5></li>	
						<li>
							<h4 id = "postBody">{{post.body | limitTo: 200}}</h4>
						</li>
					</div>
					<br>
					<button class="btn btn-danger btn-sm" ng-click = "deletePost(post)" >Delete Post</button>
					<br>
	        		</ul>

        		</div>
        </div>
        
	</div>

	
	